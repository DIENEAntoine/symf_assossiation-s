{# Navbar visitor #}
<nav class="navbar navbar-expand-lg bg-light shadow">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Kara's</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('visitor.welcome.index') }}">Acceuil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Qui somme-nous ?</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Evenements</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link">Contact</a>
                    </li>
                </ul>

                <ul class="navbar-nav ms-auto">
                    {% if app.user %}
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="">Espace admin</a>
                            </li>
                            {% elseif is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="">Espace admin</a>
                            </li>
                            {% elseif is_granted('ROLE_USER') %}
                            <li class="nav-item">
                                <a class="nav-link" href="">Espace privé</a>
                            </li>
                        {% endif %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app.logout') }}">Déconnexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('visitor.authentication.login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('visitor.registration.register') }}">Inscription</a>
                        </li>
                    {% endif %}
                </ul>

            </div>
    </div>
</nav>
